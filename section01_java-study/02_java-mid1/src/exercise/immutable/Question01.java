package exercise.immutable;

public class Question01 {
    /**
     * 문제설명
     * MyDate 클래스는 불변이 아니라서 공유 참조 시 사이드이펙트가 발생한다. 이를 불변 클래스로 만들어라
     * 새로운 불변 클래스는 ImmutableMyDate로 지으면 된다.
     * 새로운 실행 클래스는 ImmutableMyDateMain으로 이름을 지으면 된다.
     * (MyDate, MyDateMain클래스 제공)
     * */
}
